<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>


    <%= stylesheet_link_tag    "application" %>
    <%= stylesheet_link_tag    "background" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <link href='//fonts.googleapis.com/css?family=Amaranth' rel='stylesheet'>
  </head>

  <body>



      <div class="title-bar" data-responsive-toggle="mobile-menu" data-hide-for="large">
        <button class="menu-icon" type="button" data-toggle="mobile-menu"></button>
        <div class="title-bar-title"><a href="/">LeagueHQ</a></div>
      </div>

      <div class="top-bar stacked-for-medium" id="topbar-menu">
        <div class="top-bar-title">
          <a class="hide-for-small-only" href="/">LeagueHQ</a>
        </div>


        <div class="top-bar-left">
          <form id="live-search" action="" class="search-bar">
              <fieldset>
                  <input type="text" class="text-input" id="filter" value="" placeholder="Search"/>
              </fieldset>
          </form>
        </div>

        <div class="top-bar-switch" title="Turn off live themes(For slow connections)">
          <input class="switch-input" id="exampleSwitch" type="checkbox" name="exampleSwitch" checked>
          <label class="switch-paddle" for="exampleSwitch">
            <span class="switch-active" aria-hidden="true">On</span>
            <span class="switch-inactive" aria-hidden="true">Off</span>
          </label>
        </div>

        <div class="top-bar-right">
          <% if user_signed_in? %>
          <ul class="dropdown menu" data-dropdown-menu>
            <li>
              <%= link_to current_user.email, user_path(current_user.id) %>
              <ul class="menu">
                <li><%= link_to "Edit Profile", edit_user_registration_path %></li>
                <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
                </ul>
              </li>
            </ul>

            <% else %>
            <ul class="dropdown menu" data-dropdown-menu>
              <li>
                <a href="#">Login</a>
                <ul class="menu">
                  <li><%= render :partial => "devise/sessions/form" %></li>
                </ul>
              </li>
              <li>
                <%= link_to "Sign up", new_user_registration_path %>
              </li>
            </ul>
            <% end %>
          </div>
      </div>


    <%= yield %>

    <footer class="footer">
      <div class="row small-0 medium-8 large-6">
        <p>LeagueHQ isn’t endorsed by Riot Games and doesn’t reflect the views or opinions of Riot Games or anyone officially involved in producing or managing League of Legends. League of Legends and Riot Games are trademarks or registered trademarks of Riot Games, Inc. League of Legends © Riot Games, Inc.</p>
      </div>
    </footer>

  </body>
</html>
